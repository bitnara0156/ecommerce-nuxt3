export default interface BaseRangeProps {
    minValue?: number,
    maxValue?: number,
    twoSided?: Boolean,
    minimalRange?: number,
    maxRange?: number,
    limit?: number,
    minimum?: number,
};